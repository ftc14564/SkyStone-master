apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    buildToolsVersion "3.5"
    ext { minSdkVersion = 16 }


    defaultConfig {
        targetSdkVersion 28
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
